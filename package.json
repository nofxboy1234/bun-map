{
  "name": "bun-react-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "log": "git log --since='midnight'",
    "update:codex": "npm i -g @openai/codex@latest",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:reset": "bun run src/db/reset.ts",
    "db:seed": "bun run src/db/seed.ts",
    "fmt": "oxfmt",
    "lint": "oxlint --fix",
    "tsgo": "bunx --bun tsgo",
    "dev": "bun --hot src/index.tsx",
    "dev:debug": "bun --inspect-brk src/index.tsx",
    "build": "bun build --target=bun --production --outdir=dist --public-path=/ --env=BUN_PUBLIC_* src/index.tsx",
    "start:dist": "cd dist && GOODBYE=bye! bun index.js",
    "clean": "rm -rf dist && echo 'Cleaned dist.'"
  },
  "dependencies": {
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.13",
    "@types/react-dom": "^19",
    "@typescript/native-preview": "^7.0.0-dev.20260226.1",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0"
  }
}
